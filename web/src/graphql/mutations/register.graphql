mutation Register($options: UserLoginInput!) {
    register(options: $options) {
        errors {
            field
            message
        }
        user {
            ...UserFragment
        }
    }
}